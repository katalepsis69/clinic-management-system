name: Keep Render awake

on:
  schedule:
    - cron: "*/5 * * * *" # ponytail: GitHub cron min is 5 min; Render sleeps at 15
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS https://clinic-management-system-5w0e.onrender.com/
